import styled from "@emotion/styled"

import { BLOGPOSTPREVIEW_MAXWIDTH } from "@css/constants"

export const Input = styled.input`
  width: 100%;
  max-width: ${BLOGPOSTPREVIEW_MAXWIDTH};
  margin: 16px;
  padding: 4px;
`
