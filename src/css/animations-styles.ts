import { keyframes } from "@emotion/core"

export const BackgroundFlowAnimation = keyframes`
    0% { background-position: 0% 50% }
    50% { background-position: 100% 51% }
    100% { background-position: 0% 50% }
`
